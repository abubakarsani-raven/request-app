# request_app

A new Flutter project.

## Local backend

To point the app at your local backend, run with:

```bash
flutter run --dart-define=API_BASE_URL=http://YOUR_IP:4000
```

Replace `YOUR_IP` with your machine's IP (e.g. `192.168.1.1`). WebSocket URL is derived from the same base.
